{% extends 'base.html.twig' %}
 
{% block title %}Edit Invoice{% endblock %}
 
{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Edit Invoice</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('invoice_index') }}"> 
                    View All Invoices
                </a>
            </div>
            <div class="card-body">
                {% include 'invoice/form.html.twig' with {'button_label': 'Update Invoice', 'button_color': 'btn-outline-success'}  %}
            </div>
        </div>
    </div>
{% endblock %}